<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.pulmuone.batch.eon.infra.mapper.email.master.EmailBosMapper">

    <select id="getList" resultType="EmailBosVo">
        SELECT
            SN_MAIL_SEND_ID
            , MAIL
            , TITLE
            , CONTENT
            , #{senderNm} AS SENDER_NM
            , #{senderMail} AS SENDER_MAIL
            , #{returnMail} AS RETURN_MAIL
        FROM  SN_MAIL_SEND
        WHERE EON_YN = 'N'
    </select>
    <update id="update" parameterType="EmailBosVo">
        UPDATE SN_MAIL_SEND
            SET
            EON_YN = 'Y'
        WHERE SN_MAIL_SEND_ID = #{snMailSendId}
    </update>
</mapper>